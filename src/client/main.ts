import { BlackHoleIOApplication } from "./application/black-hole-io-application.js";

/**
 * Entry point of the application
 */
window.addEventListener("load", () => {
    const app = new BlackHoleIOApplication();
})